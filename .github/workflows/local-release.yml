---
name: Track releases (local)

on:
  push:
    branches:
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release-please:
    name: Run release-please
    uses: ./.github/workflows/release.yml
    with:
      tag_major_minor_versions: true
